#ifndef UTILITAIRES_CPP
#define UTILITAIRES_CPP

#include <string>

std::string lettre(int nb);

/* Purpose : To create a function that return the cell number for an one-dimensional array with coordinates of a two-dimensional array */
unsigned int coordinateOneTroughTwoDimension(int arrayNumberColumns, unsigned int currentPositionRow, unsigned int currentPositionColumn);

#endif
